# 生辰信息自动提取功能说明

## 功能简介

在八字对话页面（`/bazi`），系统现在可以从用户的对话框输入中自动提取生辰信息，包括：
- 出生日期（年月日）
- 出生时间（时分）
- 性别
- 出生地点

## 支持的输入格式

以下格式的生辰信息可以被自动识别：

### 1. 完整格式
```
1999年6月28日11时33分四川省成都市 男
公历1999年6月28日11时33分出生，性别男，地点北京市
```

### 2. 简化格式
```
1999-06-28 11:33 男
1999年6月28日 11:33 女
```

### 3. 自然语言格式
```
我是1999年6月28日11时33分出生的，男
出生于1999年6月28日11时33分，性别男
生辰八字：1999年6月28日 11:33
```

## 使用方法

1. 在八字对话页面，直接在对话框输入包含生辰信息的文本
2. 系统会自动检测并提取信息
3. 如果信息完整，会显示绿色提示框"已自动提取生辰信息"
4. 表单会自动填充提取的信息
5. **系统会自动计算八字命盘数据（mainTable）**
6. **八字数据会以JSON格式自动添加到对话上下文中**
7. AI助手会基于真实的八字数据进行分析和回答

## 功能特点

- **自动识别**：无需特定格式，自然表达即可
- **智能提示**：提取成功后显示友好的提示信息
- **信息补全**：提取到部分信息时也会提示用户
- **表单联动**：自动填充到生辰表单中
- **八字计算**：提取完整生辰信息后自动计算八字命盘数据
- **上下文传递**：八字数据自动添加到对话上下文中，AI可以基于真实的八字数据进行分析

## 实现细节

### 核心文件

- `src/lib/baziParser.ts` - 生辰信息解析工具
- `src/lib/baziGenerator.ts` - 八字数据生成工具（生成mainTable）
- `src/components/BaziForm.tsx` - 八字表单组件（支持initialData）
- `src/app/bazi/page.tsx` - 八字页面主组件
- `src/app/api/baziceishi/route.ts` - 八字对话API（接收systemPrompt）

### 主要函数

1. **extractBirthInfo(text: string)** - 从文本提取生辰信息
2. **convertToBaziFormData(birthInfo)** - 转换为表单数据格式
3. **formatBirthInfo(birthInfo)** - 格式化为显示文本

## 提取规则

### 日期提取
- 年份：识别 1990-2099 范围内的4位数字
- 月份：识别 1-12 范围内的数字
- 日期：识别 1-31 范围内的数字

### 时间提取
- 小时：0-23
- 分钟：0-59

### 性别提取
- 支持中文：男、女
- 支持英文：male、Male、female、Female

### 地点提取
- 识别中国省份、自治区、直辖市

## 示例

输入：
```
1999年6月28日11时33分四川省成都市 男，帮我分析一下我的事业发展
```

系统响应：
- ✓ 已自动提取生辰信息
- 1999年 6月 28日 11时 33分 男 四川省
- 表单自动填充
- **系统自动计算八字命盘数据**
- **八字数据（mainTable JSON）自动添加到对话上下文**
- AI基于真实的八字数据进行分析和回答

## 注意事项

- 如果日期格式不标准，可能无法准确提取
- 建议使用完整的年月日时分信息
- 提取失败时会显示"信息不完整"提示
- 八字数据计算需要完整的生辰信息（年月日时分+性别）
- 八字数据会自动包含在对话上下文中，AI可以基于此进行准确分析

## 技术实现

### 数据流

1. **输入** → 用户输入文本（包含生辰信息）
2. **提取** → `extractBirthInfo()` 提取结构化信息
3. **转换** → `convertToBaziFormData()` 转换为表单数据
4. **计算** → `generateBaziMainTable()` 计算八字命盘
5. **序列化** → `JSON.stringify()` 转换为JSON字符串
6. **发送** → 作为 `systemPrompt` 发送到API
7. **分析** → AI基于八字数据进行分析

